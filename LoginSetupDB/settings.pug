doctype html
head
  title  Settings Page 
  meta(name='description' content='iSked website')
  meta(name='author' content='Team iSked')
  link(rel='stylesheet' href='../css/settings.css')
  link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' rel='stylesheet' integrity='sha384- 1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3' crossorigin='anonymous')
section.Heading(style='background-color: #02134c;')
  h2(style='color: white; text-align: center;')  Setting 
div
  form(method='post' action='http://localhost:3000/api/store' enctype="multipart/form-data")
    #display_image
      //- var x = #{session.avatar}
      p (Only PNG images allowed)
      input#image_input(type='file' name='avatar[]')
      img(src= `${session.avatar}`, alt="User profile picture" width = '35%') 

      
      
    script.
      const image_input = document.querySelector("#image_input");
      var uploaded_image;
      image_input.addEventListener('change', function() {
      const reader = new FileReader();
      reader.addEventListener('load', () => {
      uploaded_image = reader.result;
      //- document.querySelector("#display_image").style.backgroundImage = `url(${uploaded_image})`;
      });
      reader.readAsDataURL(this.files[0]);
      });
    label(for='fname') Username 
    .boxed  #{username}
    label(for='fname') Email 
    .boxed  #{email}
    label(for='fname') Change Password: 
    input(type='text' name='password' placeholder='Enter New Password OR Existing password to change the profile picture')
    input(type='submit' value='Submit')

